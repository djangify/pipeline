DEBUG=True
SECRET_KEY=change-me
ALLOWED_HOSTS=127.0.0.1,localhost
PRIMARY_DOMAIN=http://127.0.0.1:8000
FOLLOWUP_REMINDER_TO=
DEFAULT_FROM_EMAIL=pipeline@example.com

# Used by crm.management.commands.create_default_user to seed a login on a
# fresh install of the packaged desktop app (see desktop.py).
DEFAULT_USER_EMAIL=admin@example.com
DEFAULT_USER_PASSWORD=admin123
